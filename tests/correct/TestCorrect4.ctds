Program {
    void print_int(integer i) extern ;
    integer get_int() extern ;

    void main() {
        integer y = get_int();
        
        if (y > 0) then {
            print_int(y);
        }
        return;
    }
}
