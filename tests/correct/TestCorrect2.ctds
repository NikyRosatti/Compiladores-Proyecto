Program {
    integer compute(integer a, integer b, integer c, integer d, integer e, integer f, integer g) {
        integer x = a + b;
        integer y = c + d;
        
        if (x > y) then {
            integer temp = x;
            x = y;
            y = temp;
        }
        
        integer result = x + y + e + f + g;
        return result;
    }
    
    void main() {
        integer answer = compute(1, 2, 3, 4, 5, 6, 7);
        return;
    }
}